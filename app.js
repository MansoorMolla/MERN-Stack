// callback, promises, async/await
// call, bind, apply
// callby value, call by refrence
// shallow copy, deep copy
// map, filter, reducer, some, every, find
// for, for-in, for-of, for-Each, while, do-while, switch, break, continue
// closure, currying
// Destucturing, spread, rest, default
// Generator function
// pure and impure function
// symbol, Bigint
// prototype
// Object all Prop and methods
// DOM, Event Handling
// Event, Event handling, Event handler, event propagation, event bubling, event capturing
// this keyword
// pollyfill
// debouncing, throtlling
// if it return more than one value can we use return (rest function)
// programms(delete, reverse,flatten, countoccurence, max, min, assending, desecnding, second max, fact)
// prime, fibbonanci , sum of digits in num 



// find, findLast, findIndex, findLastIndex
// indexOf, lastIndexOf

           //0123456789
const str = "JavaScript";

// substr(start, length)
console.log(str.substr(4, 6));     // "Script"
console.log(str.substr(4));        // "Script"
console.log(str.substr(-6, 3));    // "Scr"
console.log(str.substr(4, -2));    // "" (length negative = empty string)

// substring(start, end)
console.log(str.substring(4, 10)); // "Script"
console.log(str.substring(4));     // "Script"
console.log(str.substring(10, 4)); // "Script" (swaps 10 and 4 internally)
console.log(str.substring(-4));    // "JavaScript" (negative treated as 0)



// accces value in str = at , charAt, str[] 
// access value in arr = arr[] , arr.find, findLast, includes


// access index str = indexOf, lastIndexOf
// access index arr = indexOf, lastINdexof, findIndex, findLastIndex 




